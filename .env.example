# Server-side only. NEVER prefix this with VITE_ — that would leak it into the
# public browser bundle. This is read by server/index.js at runtime.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/XXXXXXX/YYYYYYY

# Optional: port the Node server listens on (default 3001)
# PORT=3001
